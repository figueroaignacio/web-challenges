export type Country = {
  flags: {
    png: string;
  },
  name: {
    common: string
  }
  population: number;
  region: string;
  capital: string;
  maps: {
    googleMaps: string;
  }
  slug: string
}